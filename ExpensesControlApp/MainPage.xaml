<?xml version="1.0" encoding="utf-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Class="ExpensesControlApp.MainPage"
    Title="Minhas Despesas">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="CRIAR"
                     Clicked="createExpenseClicked" />
    </ContentPage.ToolbarItems>

    <StackLayout>
        <Label
            Margin="0,15,0,0"
            Text="Gasto Total:"
            HorizontalOptions="Center"
            FontSize="20.0"/>

        <Label
            x:Name="total"
            Margin="0,0,0,0"
            HorizontalOptions="Center"
            FontSize="38.0"/>

        <ListView
            x:Name="listView"
            Margin="10"
            ItemSelected="didTapItemListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding expenseTitle}"
                              Detail="{Binding expenseAmount, StringFormat='R$ {0:F0}'}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>